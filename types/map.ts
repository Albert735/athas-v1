/** Current mode of the map bottom sheet. */
export type SheetState = "details" | "directions" | "navigating";

/** Mapbox Directions API transport mode. */
export type TransportProfile = "walking" | "driving" | "cycling";
